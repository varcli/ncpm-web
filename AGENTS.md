# NCPM 开发规则

本仓库是单节点 Nginx、ACME 与 Docker 运维控制面。变更必须优先保证数据面连续、配置可恢复和凭据不泄漏。

## 开发基线

- 使用 `.NET 10`，SDK 版本遵循 `global.json`。
- 完整验证命令：`dotnet restore Ncpm.slnx`、`dotnet test Ncpm.slnx -c Release --no-restore`。
- 提交前必须保持 Release 构建零警告；CI 将警告视为错误。
- 不修改或删除用户的 `data/`、`deploy/data/`、证书、密钥及未关联的工作区改动。
- 新功能必须同时完成服务端行为、错误反馈、持久化、权限检查和最小测试；禁止只增加可点击但无行为的 UI。OIDC 是当前唯一明确保留的未实现例外，界面必须标注不可用。

## 架构边界

- `config.yml` 与各类 YAML 文件是配置事实来源；进程内缓存只能加速读取，不得成为唯一状态。
- Nginx 是数据面。面板重启不得中断已经加载的代理。
- 当前范围是单实例、单节点。不要假设共享文件系统、分布式锁或多副本一致性。
- 后台周期任务必须防重入、捕获顶层异常，并在 `Dispose`/停止时释放定时器和订阅。

## 安全与可靠性不变量

- 不得加入固定默认密码。首次凭据只能来自显式 Secret/环境变量或安全随机生成，并强制首次改密。
- Bearer Token、DNS API 凭据、通知 Token、私钥和密码不得写入日志。会话只持久化单向摘要。
- 含敏感信息的文件采用原子替换；Linux 上权限应限制为所有者读写，敏感目录限制为所有者访问。
- 所有进入 Nginx 配置的域名、路径、Header、上游和标识符必须经过 `NginxConfigValidator`。
- 发布顺序必须是：生成候选配置 → 语义校验 → `nginx -t` → 快照/原子替换 → reload；任何失败恢复最后已知可用版本。
- 证书激活前必须验证证书与私钥配对。DNS-01 凭据只进入子进程环境，不得成为命令参数。
- `X-Forwarded-*` 只接受 `Panel.TrustedProxies` 中的直接代理，禁止信任任意来源。
- Docker Socket 等同于宿主机高权限。远程 Docker 默认使用 TLS；mTLS 证书路径必须成对校验。不要静默降级到明文。
- 外部 HTTP 调用必须设置超时、限制重试和队列长度。一次渠道失败只能重试该渠道，不能重复投递到成功渠道。
- 运行时静态资源必须随应用发布，不得新增公共 CDN 依赖。

## 文件与配置

- 任何可恢复状态写入都优先使用“同目录临时文件 + 原子移动”。
- UI 编辑配置时使用脱离缓存的副本；点击保存前不得改变线上行为。
- 配置项要说明是热更新还是重启生效。启动地址、BasePath、可信代理和日志 Sink 属于重启生效。
- 新增配置字段必须提供安全默认值、YAML/环境变量兼容和校验错误信息。
- 路径拼接必须限定在预期根目录，标识符不得直接作为未经校验的相对路径。

## 测试要求

- 修复缺陷时增加能复现问题的测试；安全边界至少覆盖成功和拒绝路径。
- 反代变更覆盖 Nginx 指令注入、端口/域名/路径校验。
- 认证变更覆盖首次启动、改密、会话撤销和落盘不含明文 Token。
- ACME/DNS 变更不得在普通单元测试中访问真实 CA 或 DNS；用隔离临时目录测试状态事务，真实签发按上线清单人工验收。
- Docker 镜像由 CI 构建；本机没有 Docker 时应明确记录未执行，不能声称通过。

## 文档同步

- 用户可见配置、部署步骤、端口、Secret、健康检查或限制变化时，同步更新 `README.md`、`deploy/README.md` 和 `docs/PRODUCTION-CHECKLIST.md`。
- 发布镜像版本使用上海时区日期 `yyyyMMdd`，同时保留不可变的 `sha-*` 标签用于回滚。
